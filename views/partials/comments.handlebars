<section class="section-secondary">
  {{#each this}}
  <div class="comment">
    <div class="meta">
      {{user.name}} on {{format_date date}}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </div>
  {{/each}}
</section>